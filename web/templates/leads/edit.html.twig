{% extends 'base.html.twig' %}

{% block title %}Edit Leads{% endblock %}

{% block body %}
    <h1>Edit Leads</h1>

    {{ include('leads/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('leads_index') }}">back to list</a>

    {{ include('leads/_delete_form.html.twig') }}
{% endblock %}
